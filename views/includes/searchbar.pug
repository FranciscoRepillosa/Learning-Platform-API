.header
    .dropdown(data-dropdown='')
        button.link(data-dropdown-button='') 🤓
        .dropdown-menu.information-grid
            div
                .dropdown-heading Actions
                .dropdown-links
                    a.link(href='/') 🏠
                    a.link(href='/user/login') Login
                    a.link(href='/courses/new') Create Course
                    a.link(href='/user/mycourses') MyCourses

.top-nav-container
    input#searchBox(className="search-box" type="text" placeholder="search courses and hit enter")